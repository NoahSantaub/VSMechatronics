#include <stdio.h>
#include <stdlib.h>

int main(void)
{
    // Declare some variables
    int a = 1, b = 2;
    float f = 1.23f;

    // Use printf display text on the screen
    printf ("The variables are\n a=%d\n b=%d\n f=%f\n", a, b, f);

    // Exit from main
    return 0;
}